import 'package:flutter/material.dart';

class FindingPage extends StatelessWidget {
  const FindingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("FindingPage Page"),
    );
  }
}
